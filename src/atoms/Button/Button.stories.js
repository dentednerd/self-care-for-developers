import React from 'react';
import Button from '.';

export default {
  title: 'atoms/Button',
  component: Button
};

const Template = () => (
  <Button>
    I am Button
  </Button>
);

export const Default = Template.bind({});
