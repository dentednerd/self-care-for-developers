import React from 'react';
import CookieLove from '.';

export default {
  title: 'undraws/CookieLove',
  component: CookieLove
};

const Template = () => <CookieLove />;

export const Default = Template.bind({});
